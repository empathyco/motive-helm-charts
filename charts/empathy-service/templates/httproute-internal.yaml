{{- if and .Values.httpRoutes.enabled .Values.httpRoutes.internal.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "empathy-service.fullname" . }}-internal
  labels:
    {{- include "empathy-service.labels" . | nindent 4 }}
  {{- with .Values.httpRoutes.internal.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    - group: {{ .Values.httpRoutes.internal.parentRef.group | default "gateway.networking.k8s.io" }}
      kind: {{ .Values.httpRoutes.internal.parentRef.kind | default "Gateway" }}
      name: {{ .Values.httpRoutes.internal.parentRef.name | default "internal-envoy-gateway"}}
      namespace: {{ .Values.httpRoutes.internal.parentRef.namespace | default "kube-system" }}
  {{- with .Values.httpRoutes.internal.hostnames }}
  hostnames:
    {{- range . }}
    - {{ . | quote }}
    {{- end }}
  {{- end }}
  {{- if .Values.httpRoutes.internal.rules }}
  rules:
    {{- toYaml .Values.httpRoutes.internal.rules | nindent 4 }}
  {{- else }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.httpRoutes.internal.defaultPath | default "/" | quote }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ include "empathy-service.fullname" . }}
          port: {{ .Values.service.port }}
  {{- end }}
{{- end }}
